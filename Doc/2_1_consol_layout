좋다. 그럼 **“적당히” 말고**,
📌 **SNMPc 공식 콘솔 구조를 기준으로 한 ‘콘솔 UI 기능 실현 기획서 (1차)’**를 **처음부터 끝까지 정확히** 정리한다.
이 문서는 **UI 디자이너 / WPF 개발자 / 백엔드 개발자**가 공통으로 보는 기준 문서다.

---

# 🖥 SNMPc 기반 NMS

## 1단계 : 콘솔 UI 기능 실현 기획서 (정확판)

---

## 1. 콘솔 UI의 역할 정의 (중요)

SNMPc 콘솔은 단순한 화면이 아니다.

> **“모든 관리 객체(Map, Device, MIB, Event, Trend)를
> 하나의 통합 작업 공간에서 조작하는 관리 쉘(Shell)”**

따라서 콘솔은 다음을 만족해야 한다.

* 다중 View 동시 표시 (MDI)
* 좌측 Selection Tool + 중앙 View + 하단 Event
* **모든 객체는 선택 → View 생성 → Context Menu 조작**
* 키보드/마우스 중심의 운영자 친화 UI

---

## 2. 콘솔 전체 화면 레이아웃 (고정)

📌 **SNMPc는 레이아웃이 거의 고정**이다. (이건 취향 아님)

```
┌────────────────────────────────────────────────────────┐
│ Menu Bar                                               │
├────────────────────────────────────────────────────────┤
│ Main Button Bar        | Edit Button Bar               │
├────────────────────────────────────────────────────────┤
│ Selection Tool Tabs    | View Window Area (MDI)        │
│ (Left Dock)            |                               │
│                        |                               │
│                        |                               │
├────────────────────────────────────────────────────────┤
│ Event Log Tool (Bottom Dock, 항상 표시)                │
└────────────────────────────────────────────────────────┘
```

---

## 3. Menu Bar (상단 메뉴) – **정확한 책임**

Menu는 **환경/정책/전역 기능**만 담당
👉 즉, **운영 중 자주 쓰는 기능은 Button Bar에 없음**

### 3.1 File

| 메뉴           | 기능                 |
| ------------ | ------------------ |
| Open Project | NMS 프로젝트 열기        |
| Save All     | 전체 맵/설정 저장         |
| Import       | MIB / Map / Object |
| Export       | Map / Report       |
| Exit         | 콘솔 종료              |

---

### 3.2 Edit

| 메뉴          | 기능       |
| ----------- | -------- |
| Undo / Redo | Map 편집   |
| Preferences | 콘솔 전역 옵션 |

---

### 3.3 View

| 메뉴                         | 기능       |
| -------------------------- | -------- |
| Reset Layout               | 레이아웃 초기화 |
| Show / Hide Selection Tool | 좌측 패널    |
| Show / Hide Event Log      | 하단 패널    |
| Cascade / Tile             | MDI 정렬   |

📌 **View 메뉴는 화면 배치만 다룬다**

---

### 3.4 Tools

| 메뉴               | 기능           |
| ---------------- | ------------ |
| Discover Network | 네트워크 탐지      |
| MIB Compiler     | MIB 컴파일      |
| Poll Now         | 즉시 Poll      |
| Utilities        | Ping / Trace |

---

### 3.5 Help

* Help
* About

---

## 4. Main Button Bar (상단 아이콘 바)

📌 **운영자가 하루 100번 이상 쓰는 기능**

### 4.1 Main Button Bar 구성

| 그룹         | 버튼                        |
| ---------- | ------------------------- |
| Navigation | Zoom In / Out / Pan       |
| Map        | Open Map / Back / Forward |
| Poll       | Poll Now                  |
| View       | Table / Graph / Event     |
| Utility    | MIB Browser               |

👉 버튼 클릭 = **즉시 View 생성**

---

## 5. Edit Button Bar (맵 편집 전용)

📌 **Map View가 활성화될 때만 활성화**

| 버튼         | 설명        |
| ---------- | --------- |
| Add Subnet | 서브넷 오브젝트  |
| Add Device | 장비        |
| Add Link   | 장비 연결     |
| Add Bus    | Bus 네트워크  |
| Add Ring   | Ring 네트워크 |
| Add Goto   | 맵 링크      |

👉 이 버튼들은 **Map 오브젝트 생성 전용**

---

## 6. Selection Tool (좌측 핵심 패널)

📌 **SNMPc UI의 심장**

### 6.1 탭 구조 (고정)

```
[ Map | MIB | Trend | Event | Menu ]
```

---

### 6.2 Map Tab

**모든 관리 객체의 트리**

```
Maps
 ├─ Root Map
 │   ├─ Subnet
 │   │   ├─ Device
 │   │   └─ Device
 │   └─ Link
```

* 선택 → Map View 자동 포커스
* 우클릭 → Properties / View / Delete

---

### 6.3 MIB Tab

```
MIBs
 ├─ Standard MIBs
 ├─ Vendor MIBs
 └─ Custom Expressions
```

* 선택 → Table / Graph View 생성
* 여러 장비 동시 선택 가능

---

### 6.4 Trend Tab

```
Trend Profiles
 ├─ Interface Utilization
 ├─ CPU Load
 └─ Availability
```

* 프로파일 = 리포트 정의
* 선택 → Trend View 열기

---

### 6.5 Event Tab

```
Event Filters
 ├─ All Events
 ├─ Critical
 ├─ Link Down
```

* 필터 선택 → Event Log 필터링

---

### 6.6 Menu Tab

* 사용자 정의 메뉴
* 외부 명령/스크립트 연결

---

## 7. View Window Area (중앙 MDI)

📌 **모든 작업은 View 단위**

### 7.1 View 타입

| View           | 설명       |
| -------------- | -------- |
| Map View       | 토폴로지     |
| MIB Table View | SNMP 테이블 |
| MIB Graph View | SNMP 그래프 |
| Trend View     | 장기 통계    |
| Event View     | 이벤트 리스트  |

📌 View는 **중첩 / 동시 표시 가능**

---

## 8. Event Log Tool (하단 고정)

📌 **항상 보인다 (SNMPc 핵심 특징)**

| 컬럼       | 설명                       |
| -------- | ------------------------ |
| Time     | 발생 시각                    |
| Severity | Critical / Major / Minor |
| Source   | 장비                       |
| Message  | 내용                       |

* 더블클릭 → 관련 Map/View 이동
* Ack / Clear 가능

---

## 9. UI 동작 원칙 (중요 규칙)

1. **모든 객체는 Selection Tool에서 시작**
2. Menu = 설정 / Button = 실행
3. View는 절대 하나만 강제하지 않는다
4. Event는 항상 하단에 표시
5. Map 편집과 운영 모드는 분리

---

## 10. WPF 구현 기준 매핑 (참고)

| UI 개념          | WPF                          |
| -------------- | ---------------------------- |
| 콘솔 쉘           | Window                       |
| Menu Bar       | Menu                         |
| Button Bar     | ToolBar                      |
| Selection Tool | TabControl + TreeView        |
| MDI            | TabControl / Docking Library |
| Event Log      | DataGrid                     |

---

## 다음 단계 (이제 진짜 구현 단계)

이제 선택지는 딱 3개다:

1️⃣ **콘솔 Shell WPF XAML 뼈대 바로 작성**
2️⃣ **Map View UI 상세 설계 (아이콘/링크/상태)**
3️⃣ **Selection Tool 트리 데이터 모델 설계**

👉 다음은 뭘로 갈까?
(여기까지는 “기획”이고, 다음부터는 **실제 코드 단계**다)
