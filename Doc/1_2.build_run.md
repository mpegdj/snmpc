프로젝트 구조 확인 중.


[4 tools called]


빌드 및 실행 명령어:

```bash
# 빌드
dotnet build SnmpNms.sln

# 빌드 + 실행
dotnet run --project SnmpNms.UI/SnmpNms.UI.csproj

# 또는 빌드 후 직접 실행
dotnet build SnmpNms.sln
dotnet SnmpNms.UI/bin/Debug/net9.0-windows/SnmpNms.UI.exe
```

**간단 버전:**
```bash
dotnet run --project SnmpNms.UI/SnmpNms.UI.csproj
```

**Release 빌드:**
```bash
dotnet build SnmpNms.sln -c Release
dotnet SnmpNms.UI/bin/Release/net9.0-windows/SnmpNms.UI.exe
```