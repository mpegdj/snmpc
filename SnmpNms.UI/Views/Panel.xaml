<UserControl x:Class="SnmpNms.UI.Views.BottomPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:eventLog="clr-namespace:SnmpNms.UI.Views.EventLog"
             mc:Ignorable="d" 
             d:DesignHeight="220" d:DesignWidth="800"
             MinHeight="120"
             Background="{StaticResource VSCodePanelBackground}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Panel Header with Tabs -->
        <Border Grid.Row="0" 
                Style="{StaticResource PanelHeaderStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Tabs -->
                <TabControl Grid.Column="0"
                           x:Name="tabPanel"
                           Style="{StaticResource VSCodeTabControlStyle}"
                           TabStripPlacement="Bottom"
                           BorderThickness="0">
                    <TabControl.Resources>
                        <Style TargetType="TabItem" BasedOn="{StaticResource PanelTabItemStyle}"/>
                    </TabControl.Resources>
                    
                    <TabItem Header="Event Log" x:Name="tabEventLog">
                        <!-- Event Log content will be set in code-behind -->
                    </TabItem>
                    
                    <TabItem Header="Output" x:Name="tabOutput" IsEnabled="False">
                        <TextBlock Text="Output (Coming soon)" 
                                  Foreground="{StaticResource VSCodeSecondaryForeground}"
                                  Margin="12"/>
                    </TabItem>
                    
                    <TabItem Header="Terminal" x:Name="tabTerminal" IsEnabled="False">
                        <TextBlock Text="Terminal (Coming soon)" 
                                  Foreground="{StaticResource VSCodeSecondaryForeground}"
                                  Margin="12"/>
                    </TabItem>
                </TabControl>
                
                <!-- Toggle Button -->
                <Button Grid.Column="1"
                       x:Name="btnToggle"
                       Style="{StaticResource PanelToggleButtonStyle}"
                       Click="BtnToggle_Click"
                       ToolTip="Toggle Panel">
                    <TextBlock Text="&#xE70D;" 
                              FontFamily="Segoe MDL2 Assets"
                              FontSize="12"
                              Foreground="{StaticResource VSCodeForeground}"/>
                </Button>
            </Grid>
        </Border>
        
        <!-- Panel Content -->
        <ContentControl Grid.Row="1" 
                       x:Name="contentArea"
                       Content="{Binding ElementName=tabPanel, Path=SelectedContent}"/>
    </Grid>
</UserControl>

