<UserControl x:Class="SnmpNms.UI.Views.EventLog.EventLogTabControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Storyboard x:Key="SpinnerAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="spinnerIcon"
                           Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                           From="0" To="360" Duration="0:0:1"/>
        </Storyboard>
    </UserControl.Resources>
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tool Bar Header -->
        <Border Grid.Row="0" Background="#F7F7F7" BorderBrush="{StaticResource VSCodeDivider}" BorderThickness="0,0,0,1" Padding="4,2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Hidden Spinner to satisfy code-behind reference -->
                <TextBlock x:Name="spinnerIcon" Visibility="Collapsed">
                    <TextBlock.RenderTransform>
                        <RotateTransform/>
                    </TextBlock.RenderTransform>
                </TextBlock>

                <!-- 툴바 (왼쪽) -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Content="Clear" Style="{StaticResource PanelButtonStyle}" Click="BtnEventClear_Click" 
                            ToolTip="Clear all events"/>
                    <Button Content="Copy" Style="{StaticResource PanelButtonStyle}" Click="BtnEventCopy_Click" 
                            ToolTip="Copy to clipboard"/>
                    <Button Content="Save" Style="{StaticResource PanelButtonStyle}" Click="BtnEventSave_Click" 
                            ToolTip="Save log to file"/>
                    <CheckBox x:Name="chkEventAutoScroll" Content="Auto-scroll" IsChecked="True" 
                              VerticalAlignment="Center" Margin="8,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Grid -->
        <DataGrid Grid.Row="1"
                  x:Name="dataGridLog"
                  ItemsSource="{Binding View}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="All"
                  HorizontalGridLinesBrush="{StaticResource VSCodeGridLine}"
                  VerticalGridLinesBrush="{StaticResource VSCodeGridLine}"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeRows="False"
                  RowHeaderWidth="0"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  Background="White"
                  BorderBrush="{StaticResource VSCodeDivider}"
                  BorderThickness="0">
            <DataGrid.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="TextWrapping" Value="NoWrap"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding Timestamp, StringFormat=HH:mm:ss}" Width="90"/>
                <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="90"/>
                <DataGridTextColumn Header="Device" Binding="{Binding Device}" Width="140"/>
                <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
